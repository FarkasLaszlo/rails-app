= form_for @category do |f|
  = render "layouts/errors", locals: @category
  .field
    = f.label :name, t("activerecord.attributes.category.name")
    = f.text_field :name, value: @category.name, class: "form-control"
  .actions
    = f.submit t('buttons.actions.save'), class: "btn btn-primary"
